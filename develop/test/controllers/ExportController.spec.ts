// Bellow document is testing ExportController

describe('ExportController', () => {

	const assert = require('chai').assert;
	const expect = require('chai').expect;

	it('Tests assert.equal', function() {
		assert.equal("a", "a");
	});

});
